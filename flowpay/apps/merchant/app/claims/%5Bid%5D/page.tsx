import {api} from "../../../lib/api";
async function approve(id:string){"use server";await api(`/v1/claims/${id}/approve`,{method:"POST"});}
async function fund(id:string){"use server";await api(`/v1/claims/${id}/fund`,{method:"POST"});}
export default async function ClaimDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=await api(`/v1/claims/${id}`);const rec=c.recovery;return <><div className="hero"><div><div className="eyebrow">Claim investigation</div><h1>{c.status}</h1><p className="muted mono">{c.id}</p></div><div>{c.status==="APPROVAL_PENDING"&&<form action={approve.bind(null,id)}><button className="button">Approve recovery</button></form>}</div></div><div className="cols section"><div className="card"><h2>Expected payment</h2><div className="kv"><div>Payment</div><div className="mono">{c.payment_id}</div><div>Expected</div><div>{c.expected_asset} on {c.expected_chain}</div><div>Actual claim</div><div>{c.actual_asset??"?"} on {c.actual_chain??"?"}</div><div>Transaction</div><div className="mono">{c.transaction_hash??"—"}</div><div>Destination</div><div className="mono">{c.recovery_destination}</div></div></div><div className="card"><h2>Customer explanation</h2><p>{c.explanation}</p><div className="muted">Evidence is investigation input, never authoritative.</div></div></div><div className="cols section"><div className="card"><h2>Agent investigation</h2><div className="timeline">{(c.agent?.tool_calls??[]).map((t:any,i:number)=><div className="step" key={i}><b>{t.tool}</b><div className="muted">{t.status}{t.error_class?` · ${t.error_class}`:""}</div><pre>{JSON.stringify(t.output??{},null,2)}</pre></div>)}</div></div><div className="card"><h2>Recovery plan</h2>{!rec?<p className="muted">No recovery plan yet.</p>:<div className="kv"><div>Policy</div><div>{rec.policy_decision}</div><div>Simulation</div><div><span className="pill">{rec.simulation_status}</span></div><div>Amount</div><div className="mono">{rec.amount_atomic}</div><div>Deploy receiver</div><div>{String(rec.receiver_deployment_required)}</div><div>Estimated gas</div><div>{rec.estimated_gas??"—"}</div><div>Approval</div><div>{rec.approval_status??"—"}</div><div>Recovery tx</div><div className="mono">{rec.recovery_tx??"—"}</div><div>Plan hash</div><div className="mono">{rec.plan_hash}</div></div>}{rec?.policy_decision==="NEEDS_FUNDING"&&<form action={fund.bind(null,id)}><button className="button secondary">Fund test gas</button></form>}</div></div><div className="section card"><h2>Audit-safe rationale</h2>{(c.agent?.decisions??[]).map((d:any,i:number)=><div className="step" key={i}><b>{d.rationale}</b><div className="muted">{d.verification}</div></div>)}</div></>}
